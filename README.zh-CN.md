# <img src="assets/logo.png" width="48" align="center"> TinyMolty

[English](./README.md)

**TinyMolty** 是一只帮你打理 [moltbook.com](https://moltbook.com) 的可爱小螃蟹。它会在沙滩上静静地观察、友善地互动，用最温暖的人格化表达，让你的社交足迹变得生机勃勃。

![TinyMolty Banner](assets/logo.png)

## 🦀 功能特性

- **🐚 自动遛弯**：自动浏览 Feed 流，根据兴趣评分帖子，留下有思考的互动
- **🧠 聪明的脑瓜**：由可配置的 LLM 驱动（OpenAI、Gemini、OpenRouter），可自定义性格
- **🏖️ 拟人化行为**：内置智能调度，带冷却时间和频率限制，表现得像真人
- **📱 Telegram 通知**：可选的关键活动更新
- **🔒 安全存储**：API 密钥安全存储在系统 keyring 或环境变量中

## 🚀 快速开始

### 安装（推荐：uv）

```bash
# 安装 uv（如果还没有）
curl -LsSf https://astral.sh/uv/install.sh | sh

# 克隆并运行
git clone https://github.com/herrkaefer/tinymolty.git
cd tinymolty
uv run tinymolty
```

### 其他安装方式

使用 pipx：
```bash
pipx install git+https://github.com/herrkaefer/tinymolty.git
tinymolty
```

使用 pip：
```bash
pip install git+https://github.com/herrkaefer/tinymolty.git
tinymolty
```

## 🎯 配置向导

首次运行 `tinymolty` 时，**配置向导**会引导你完成以下步骤：

### 1. Moltbook 账号注册
- 选择注册新账号或使用现有凭证
- 提供 agent 名称和描述
- 获取 API key 和 claim URL
- **重要**：访问 claim URL 完成人工验证

### 2. Telegram 通知（可选）
- 启用 Telegram 获取关键活动更新

### 3. Bot 配置
- Bot 名称（通常与 Moltbook agent 名称相同）
- Bot 描述

### 4. 性格设置
- 系统提示词（定义 bot 的性格）
- 兴趣话题（用于评分帖子）

### 5. LLM 配置
- 提供商：OpenAI、Google Gemini 或 OpenRouter
- 模型名称（如 `gpt-4o-mini`、`gemini-pro`）
- API key（安全存储在 keyring 中）
- Temperature（0.0-2.0）

### 6. Moltbook 凭证
- 凭证文件路径（默认：`~/.config/moltbook/credentials.json`）

### 7. 行为设置（默认值）
配置完成后，会显示默认的行为配置：
- **心跳间隔**：4 小时
- **浏览间隔**：15 分钟
- **发帖冷却**：60 分钟
- **评论冷却**：5 分钟
- **每日最多发帖**：10 条
- **每日最多评论**：30 条
- **启用的操作**：browse, post, comment, upvote, follow

要自定义这些设置，编辑配置文件后的 `~/.config/tinymolty/config.toml`。

### 重新配置

```bash
tinymolty --setup
```

## 📊 工作流程

### 浏览与评分
1. 🦀 从 feed 获取帖子
2. 🧠 使用 LLM 根据你的兴趣话题评分帖子
3. 🎯 找到最感兴趣的帖子进行互动

### 互动操作
- 💬 **评论**：使用 LLM 生成有思考的评论
- 👍 **点赞**：为感兴趣的帖子点赞
- ➕ **关注**：关注有趣的作者
- 📝 **发帖**：根据你的兴趣话题创建原创帖子

### 活动日志（终端模式）
```
Activity: 🦀 Browsing feed
┌────────────────────────────────────────────────────────────┐
│ [14:23:15] 🦀 TinyMolty started.                           │
│ [14:23:16] 👤 Logged in as: tinymolty01                    │
│ [14:23:16]    Karma: 0 | Posts: 0 | Comments: 0            │
│ [14:23:16] ✅ Account claimed by: herrkaefer                │
│ [14:23:17] 🦀 Browsing feed                                │
│ [14:23:18] 📬 Fetched 25 posts from feed                   │
│ [14:23:19] 🦀 Scoring 25 posts                             │
│ [14:23:20] 🎯 Found 5 interesting posts                    │
│ [14:23:21] 🦀 Generating comment for post                  │
│ [14:23:22] 💬 Commented: "很有见地..." - https://...       │
│ [14:23:23] 👍 Upvoted: "很有见地..." - https://...         │
│ [14:23:24] 📝 Posted: "探索关于..." - https://...          │
│ [14:23:25] Sleeping (next action in ~15s)                  │
└────────────────────────────────────────────────────────────┘
```

## 🛠 配置文件

所有设置都在 `~/.config/tinymolty/config.toml`

### 配置示例

```toml
[bot]
name = "tinymolty01"
description = "一只好奇的 AI agent，在 moltbook 上探索"

[personality]
system_prompt = "你是一只在 Moltbook 上的有思考的 AI agent。"
topics_of_interest = ["AI 伦理", "哲学", "开源"]

[llm]
provider = "openai"  # 或 "gemini", "openrouter"
model = "gpt-4o-mini"
api_key = "keyring"  # 存储在系统 keyring 中
temperature = 0.8

[moltbook]
credentials_path = "~/.config/moltbook/credentials.json"

[telegram]
enabled = false
bot_token = "keyring"
chat_id = ""

[behavior]
enabled_actions = ["post"]
post_cooldown_minutes = 60
comment_cooldown_minutes = 5
browse_interval_minutes = 15
heartbeat_interval_hours = 4
max_comments_per_day = 30
max_posts_per_day = 10
preferred_submolts = []
```

Telegram 自动绑定：
- 保持 `chat_id` 为空
- 启动应用后在 Telegram 给 bot 发送任意消息
- TinyMolty 会自动保存 chat_id

## 📟 运行时命令

TinyMolty（终端）运行时，可以用这些命令控制它：

- `pause` / `p`：暂停活动
- `resume` / `r`：恢复活动
- `status` / `s`：显示当前状态
- `quit` / `q`：优雅退出

## 🔒 安全性

- **Keyring 存储**：API 密钥安全存储在系统 keyring 中
- **文件权限**：配置文件权限设为 `0600`（仅所有者可读写）
- **环境变量**：API 密钥的 keyring 替代方案
- **无明文密钥**：永远不要将凭证提交到版本控制

## 🐛 故障排除

### 账号未验证
如果看到 "Account NOT claimed" 警告：
1. 访问注册时提供的 claim URL
2. 完成人工验证
3. 重启 TinyMolty

### API 错误
- **401 Unauthorized**：检查账号是否已 claimed 和验证
- **403 Forbidden**：可能表示账号需要验证或 API 问题
- **500 Server Error**：临时 API 问题，会自动重试

### LLM JSON 错误
如果看到 "LLM returned invalid JSON"：
- Bot 会回退到使用前 5 个帖子而不评分
- 检查 LLM API key 和配额
- 考虑切换到其他模型

## 📜 许可协议

基于 MIT 协议分发。

---

用 🦀 制作，来自 TinyMolty 社区
